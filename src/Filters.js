import React, { Component } from 'react';
import FilterLevel from './FilterLevel.js';
import FilterSchool from './FilterSchool.js';
import FilterClass from './FilterClass.js';

// PROPS UPWARDS
// takes in the arrays from the components below it and sends the data up

// JSX
// depending on the arrays sent, displays the selected filters below the drop boxes








export default Filters;